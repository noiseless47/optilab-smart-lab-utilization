# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lab_resource_monitor
DB_USER=postgres
DB_PASSWORD=your_password_here

# Collection Credentials for Target Systems
# SSH Configuration (Linux systems)
SSH_USERNAME=monitor
SSH_PASSWORD=your_ssh_password_here
SSH_KEY_PATH=/path/to/ssh/key  # Optional: Use key-based auth instead of password

# WMI Configuration (Windows systems)
WMI_USERNAME=administrator
WMI_PASSWORD=your_wmi_password_here

# SNMP Configuration (Universal)
SNMP_COMMUNITY=public

# Optional: RabbitMQ Configuration
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# Optional: API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Optional: Logging
LOG_LEVEL=INFO
